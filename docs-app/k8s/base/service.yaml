apiVersion: v1
kind: Service
metadata:
  name: calangoui-docs
  labels:
    app.kubernetes.io/name: calangoui-docs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: calangoui-docs
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
