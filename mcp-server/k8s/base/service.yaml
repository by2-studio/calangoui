apiVersion: v1
kind: Service
metadata:
  name: calangoui-mcp
  labels:
    app.kubernetes.io/name: calangoui-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: calangoui-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
